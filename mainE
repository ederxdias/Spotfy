#make do git
#comando para executar "make -f mainE"
BRANCH := eder
MENSAGEM := Alteracao

all: push

push: commit
	@git push origin $(BRANCH)

commit: add
	@git commit -m "$(MENSAGEM)"
	
add:
	@git add .
